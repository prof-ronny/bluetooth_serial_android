name: bluetooth_serial_android_example
description: >
  Example app demonstrating how to use the bluetooth_serial_android plugin.
publish_to: "none"

environment:
  sdk: ">=2.17.0 <4.0.0"
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter

  bluetooth_serial_android:
    path: ../

  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter
  flutter_lints: ^3.0.0   # Compatível com Dart 2.17+

flutter:
  uses-material-design: true
